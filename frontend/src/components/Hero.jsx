import hero from "../assets/hero.png";

const Hero = () => {
  return (
    <div className="container mx-auto mt-70 flex justify-center items-center">
      <img src={hero} />
    </div>
  );
};

export default Hero;
